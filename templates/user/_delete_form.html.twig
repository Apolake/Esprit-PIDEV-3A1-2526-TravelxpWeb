<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" class="inline-form" onsubmit="return confirm('Delete this user account?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
