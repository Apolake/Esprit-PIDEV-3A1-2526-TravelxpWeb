{% extends 'base.html.twig' %}

{% block title %}{{ isAdmin ? 'Admin Menu - New Property' : 'New Property' }}{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="">
{% endblock %}

{% block body %}
    {% set routePrefix = isAdmin ? 'admin_property_' : 'property_' %}
    <section class="glass-card">
        <p class="eyebrow">{{ isAdmin ? 'Admin Menu' : 'Front Office' }}</p>
        <h1>Create property</h1>
        <p class="muted">Publish a property listing with full validation.</p>
        {% if not geoapifyMapTilesApiKeyConfigured %}
            <div class="flash flash-warning">Set your Geoapify keys in .env.local to enable autocomplete, geocoding, maps, places, and routing.</div>
        {% endif %}

        {{ include('property/_form.html.twig', {
            button_label: 'Create property',
            geoapifyMapTilesApiKey: geoapifyMapTilesApiKey,
            geoapifyMapTilesUrl: geoapifyMapTilesUrl
        }) }}

        <a class="btn" href="{{ path(routePrefix ~ 'index') }}">Back to list</a>
    </section>
{% endblock %}
