# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
<<<<<<< Updated upstream
=======
    geoapify_map_tiles_api_key: '%env(string:GEOAPIFY_MAP_TILES_API_KEY)%'
    geoapify_map_tiles_url: '%env(string:GEOAPIFY_MAP_TILES_URL)%'
    geoapify_autocomplete_api_key: '%env(string:GEOAPIFY_AUTOCOMPLETE_API_KEY)%'
    geoapify_places_api_key: '%env(string:GEOAPIFY_PLACES_API_KEY)%'
    geoapify_routing_api_key: '%env(string:GEOAPIFY_ROUTING_API_KEY)%'
    geoapify_autocomplete_url: '%env(string:GEOAPIFY_AUTOCOMPLETE_URL)%'
    geoapify_places_url: '%env(string:GEOAPIFY_PLACES_URL)%'
    geoapify_routing_url: '%env(string:GEOAPIFY_ROUTING_URL)%'
    exchange_rate_api_key: '%env(string:EXCHANGE_RATE_API_KEY)%'
    exchange_rate_api_url: '%env(string:EXCHANGE_RATE_API_URL)%'
>>>>>>> Stashed changes

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

<<<<<<< Updated upstream
=======
    App\Controller\PropertyController:
        arguments:
            $geoapifyMapTilesApiKey: '%geoapify_map_tiles_api_key%'
            $geoapifyMapTilesUrl: '%geoapify_map_tiles_url%'

    App\Service\GeoapifyService:
        arguments:
            $autocompleteApiKey: '%geoapify_autocomplete_api_key%'
            $placesApiKey: '%geoapify_places_api_key%'
            $routingApiKey: '%geoapify_routing_api_key%'
            $autocompleteUrl: '%geoapify_autocomplete_url%'
            $placesUrl: '%geoapify_places_url%'
            $routingUrl: '%geoapify_routing_url%'

    App\Service\CurrencyConverterService:
        arguments:
            $exchangeRateApiKey: '%exchange_rate_api_key%'
            $exchangeRateApiUrl: '%exchange_rate_api_url%'

    App\Service\GeminiAssistantService:
        arguments:
            $apiKey: '%env(string:GEMINI_API_KEY)%'
            $model: '%env(string:GEMINI_MODEL)%'

>>>>>>> Stashed changes
    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones
